'use strict';
// Файловые операции в обход Electron.
//
// Electron подменяет работу с путями, содержащими «.asar»: он выдаёт
// архив за папку. Установщику же нужен именно файл — целиком, чтобы
// прочитать заголовок, скопировать и вернуть обратно.

module.exports = (() => {
  try {
    return require('original-fs');
  } catch {
    return require('fs');
  }
})();
